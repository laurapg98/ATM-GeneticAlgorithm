%% SECTION 1: limpiar lo que tengamos anterior
clc
clear all
close all

%% SECTION 2: leemos sector y vuelos
fSector="sector"; % fichero datos sector
fFP="FlightPlans"; % fichero datos vuelos
listaFP=LeerFP(fFP); % da una matriz de FP a partir del fichero datos vuelos

%% SECTION 3: Generamos población inicial
Poblacion=100;
i=1;
Matriz_poblacion=zeros(Poblacion,Nvuelos);
while i<=Poblacion    
    Matriz_poblacion(i,:)=UnaSolucionRandom(Nvuelos);
end
